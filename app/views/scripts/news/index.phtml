<div class="field-bg">
	<h2 class="title-bigger text-center">новости</h2>
	<? foreach( $this->news as $item ): ?>
		<article>
			<header>
				<h3 class="title">
					<a class="title" name="<?= sprintf("view%d", $item->id); ?>" href="<?php echo $this->url(array('id' => $item->id ), 'staticNewsView', true) ?>">
						<?= $this->escape($item->title); ?>
					</a>
				</h3>
				<date class="date-bigger date-news">
					<?= $this->prepareConcertDate($item->date_publish); ?>
				</date>
			</header>
			<?= $item->content; ?>
			<footer>
				<p class="signature"><?= $this->escape($this->moder_author); ?></p>
			</footer>
		</article>
	<? endforeach; ?>
</div>