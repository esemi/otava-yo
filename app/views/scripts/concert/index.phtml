<div class="field-bg">
	<h2 class="title text-center">ближайшие концерты</h2>
	<? foreach( $this->concerts as $item ): ?>
		<article class="concert-block">
			<header>
				<date class="date-bigger date-concert">
					<?= $this->prepareConcertDate($item->date); ?>
					<div class="time">
						<?= ( !empty($item->time) ) ? $this->escape($item->time) : ''; ?>
					</div>
				</date>
				<h3 class="concert-name"><?= $this->escape($item->title); ?></h3>
				<? if( !empty($item->place) ): ?>
					<p class="concert-place"><?= $this->escape($item->place); ?></p>
				<? endif; ?>
			</header>
			<p><?= $this->escape($item->desc); ?></p>
			<? if( !empty($item->link) ): ?>
				<p><a href="<?= $item->link ?>" rel="nofollow">подробнее</a></p>
			<? endif; ?>
			<? if( !empty($item->cost) ): ?>
				<p>Билеты: <?= $this->escape($item->cost); ?>р.</p>
			<? endif; ?>
		</article>
	<? endforeach; ?>

	<h2 class="title text-center">прошедшие концерты</h2>
	<? foreach( $this->oldConcerts as $item ): ?>
		<article class="concert-block">
			<header>
				<date class="date-bigger date-concert">
					<?= $this->prepareConcertDate($item->date); ?>
					<div class="time">
						<?= ( !empty($item->time) ) ? $this->escape($item->time) : ''; ?>
					</div>
				</date>
				<h3 class="concert-name"><?= $this->escape($item->title); ?></h3>
				<? if( !empty($item->place) ): ?>
					<p class="concert-place"><?= $this->escape($item->place); ?></p>
				<? endif; ?>
			</header>
			<p><?= $this->escape($item->desc); ?></p>
			<? if( !empty($item->link) ): ?>
				<p><a href="<?= $item->link ?>" rel="nofollow">подробнее</a></p>
			<? endif; ?>
			<? if( !empty($item->cost) ): ?>
				<p>Билеты: <?= $this->escape($item->cost); ?>р.</p>
			<? endif; ?>
		</article>
	<? endforeach; ?>
</div>
